name: Build & Publish Docker

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository }}/traefik-cert-watcher:latest
          docker build -t $IMAGE ./cmd/traefik-cert-watcher

      - name: Push Docker image
        run: |
          IMAGE=ghcr.io/${{ github.repository }}/traefik-cert-watcher:latest
          docker push $IMAGE
